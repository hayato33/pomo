import { Work_Sans, M_PLUS_1 } from 'next/font/google';

export const workSans = Work_Sans({
  subsets: ['latin'],
  display: 'swap',
});
export const mPlus1 = M_PLUS_1({
  subsets: ['latin'],
  display: 'swap',
});
